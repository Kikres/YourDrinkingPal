@using BusinessLayer.Models.DTO;
@model List<DrinkDto>

<div class="bg-white">
    <div class="mx-auto max-w-2xl px-4 py-16 sm:px-6 sm:py-24 lg:max-w-7xl lg:px-8">
        <div class="grid grid-cols-1 gap-6 gap-y-10 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 xl:gap-8">
            @foreach (var drink in Model)
            {
                <partial name="_DrinkCard" for="@drink"/>
                
            }
        </div>
    </div>
</div>